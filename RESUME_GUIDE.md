# 💻 다른 컴퓨터에서 이어서 작업하기 (매뉴얼)

이 매뉴얼은 안티그라비티(Antigravity)와 함께 진행하던 프로젝트를 다른 컴퓨터에서 매끄럽게 이어가기 위한 절차를 설명합니다.

---

## 1단계: 코드 가져오기 (Git)

새로운 컴퓨터에 프로젝트 코드를 먼저 세팅해야 합니다.

1.  **터미널(Terminal)**을 엽니다.
2.  프로젝트를 저장하고 싶은 위치로 이동한 후, 다음 명령어를 실행합니다:
    ```bash
    git clone https://github.com/pyw31337/culture.git
    ```
3.  이미 폴더가 있다면, 최신 상태로 업데이트합니다:
    ```bash
    git pull
    ```

---

## 2단계: 개발 환경 구성 (Dependency)

프로젝트에 필요한 도구들을 설치합니다.

1.  프로젝트 폴더 안으로 이동합니다:
    ```bash
    cd CultureFlow-New
    ```
2.  필수 패키지들을 설치합니다:
    ```bash
    npm install
    ```

---

## 3단계: 안티그라비티 실행 및 히스토리 공유

안티그라비티는 프로젝트 폴더를 열면 자동으로 코드를 분석합니다.

1.  **안티그라비티(Antigravity) 앱**을 실행합니다.
2.  **[Open Folder]**를 눌러 `CultureFlow-New` 폴더를 선택합니다.
3.  **히스토리 파악 요청:** 채팅창에 다음과 같이 입력하세요:
    > ".agents/history 폴더에 있는 마지막 작업 기록들을 읽고, 내가 마지막에 어디까지 했는지 그리고 다음에 뭘 해야 하는지 브리핑해줘."

---

## 4단계: 작업 재개 (Verification)

안티그라비티가 상황을 파악하면, 다음 명령어로 데이터 상태를 최종 확인하고 작업을 시작할 수 있습니다.

1.  **데이터 생성 테스트:**
    ```bash
    npm run generate-data
    ```
2.  **데이터 정합성 검사:**
    ```bash
    npx tsx scripts/validate-data-integrity.ts
    ```

---

## 💡 팁
- **Brain 파일 백업:** 현재 `.agents/history` 폴더에 작업 기록(계획서, 결과보고서 등)이 Git을 통해 동기화되어 있으므로, 다른 컴퓨터에서도 제가 이전의 생각을 그대로 읽어낼 수 있습니다.
- **연속성:** 컴퓨터를 옮길 때마다 항상 **`git push`**와 **`git pull`**을 습관화하시면 가장 안전합니다.
